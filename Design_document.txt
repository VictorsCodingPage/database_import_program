Importeringsmodul specification:
	Parsing:
		Non-standard
	
	Data retrieval:
		get_store_name_list(cursor): 				List
		get_store_id_list(cursor):				List
		get_product_id_from_products(product_code, cursor):	Int
		get_store_id_from_stores(vendor_name, cursor):		Int
		get_variant_id_from_variants(product_id, cursor):	Int
	
	Data sending:
		insert_store_into_stores(store_name, cursor):		Boolean
		export_data_to_database(list_of_products, cursor):	Boolean

	Data verification:
		file_exemption_filter(files_to_exempt_file, files_to_filter_list, delimiter):	List
			update_exemption_filter(exempt_files_file, file_name, delimiter):	Boolean
		
	Data reading:
		scan_file_to_list(file, delimiter):	List

	DTOs:
		mProduct(object): def __init__(self, variant, **kwargs), def __repr__(self);
		mVariant(object): def __init__(self, **kwargs);
		
	Main:
		import statements
		choose folder to read files from
		retrieve files list from folder
		
				